DATABASE_URL="postgresql://..."
DATABASE_URL_UNPOOLED="postgresql://..."
ADMIN_PASSWORD="pick-a-password"
ADMIN_SECRET="run: openssl rand -hex 32"

# Optional. Powers the "attach a gif" picker on /confess. Works out of the
# box using Giphy's public demo key, but that key is shared and rate
# limited — grab your own free key at https://developers.giphy.com and
# drop it in here for real traffic.
NEXT_PUBLIC_GIPHY_API_KEY=""
